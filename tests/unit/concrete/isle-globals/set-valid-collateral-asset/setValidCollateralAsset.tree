setValidReceivableAsset.t.sol
├── when the caller is not governor
│  └── it should revert
└── when the caller is governor
   ├── it should set the valid receivable asset
   └── it should emit a {ValidReceivableAssetSet} event