triggerDefault.t.sol
├── when the function is paused
│  └── it should revert
└── when the function is not paused
   ├── when the sender is not admin or governor
   │  └── it should revert
   └── when the sender is admin or governor
      ├── it should do triggerDefault
      └── it should handle cover and emit {CoverLiquidated} event