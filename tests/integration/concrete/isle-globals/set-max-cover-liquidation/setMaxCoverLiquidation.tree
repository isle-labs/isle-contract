setMaxCoverLiquidation.t.sol
├── when the caller is not governor
│  └── it should revert
└── when the caller is governor
   ├── it should emit a {MaxCoverLiquidationSet} event
   └── it should set the max cover liquidation for the specific pool configurator