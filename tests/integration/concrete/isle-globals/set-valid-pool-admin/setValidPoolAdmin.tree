setValidPoolAdmin.t.sol
├── when the caller is not governor
│  └── it should revert
└── when the caller is governor
   ├── it should set the valid pool admin
   └── it should emit a {ValidPoolAdminSet} event