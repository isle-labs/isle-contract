setPoolConfigurator.t.sol
├── when the caller is not governor
│  └── it should revert
└── when the caller is governor
   ├── when the input pool admin is not a valid pool admin
   │  └── it should revert
   └── when the input pool admin is a valid pool admin
      ├── when the input pool admin has already owned a pool configurator
      │  └── it should revert
      └── when the input pool admin has not owned a pool configurator
         ├── when the input pool configurator is zero address
         │  └── it should revert
         └── when the input pool configurator is not zero address
            ├── it should set the pool configurator to the pool admin
            └── it should emit a {PoolConfiguratorSet} event
