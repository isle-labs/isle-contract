setContractPaused.t.sol
├── when the caller is not governor
│  └── it should revert
└── when the caller is governor
   ├── it should set the contract paused state
   └── it should emit a {ContractPausedSet} event